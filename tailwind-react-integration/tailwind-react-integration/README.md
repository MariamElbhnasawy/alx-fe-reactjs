# React JS + CSS User Profile Card

This is a simple React project using Vanilla CSS (no Tailwind) to create a responsive and interactive User Profile Card.

## Features

- Responsive layout
- Hover effects
- Smooth transitions

## Run the app

```bash
npm install
npm run dev
```
